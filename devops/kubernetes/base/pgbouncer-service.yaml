apiVersion: v1
kind: Service
metadata:
  name: pgbouncer
  namespace: healthcare-backend
  labels:
    app: pgbouncer
spec:
  type: ClusterIP
  selector:
    app: pgbouncer
  ports:
  - port: 6432
    targetPort: 6432
    name: pgbouncer

